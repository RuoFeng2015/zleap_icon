name: Deploy Preview Site to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'src/icons/**'
      - 'svg/**'
      - 'icons.json'
      - 'icons-manifest.json'
  workflow_run:
    workflows: ['Publish to NPM', 'Sync Icons from Figma', 'Build Icons']
    types:
      - completed
    branches: [main]
  workflow_dispatch: # 允许手动触发

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check if icons exist
        run: |
          echo "Checking for icons..."
          ls -la svg/ || echo "No svg directory"
          ls -la icons.json || echo "No icons.json"
          echo "SVG count: $(find svg -name "*.svg" 2>/dev/null | wc -l)"

      - name: Generate icons if needed
        run: |
          if [ ! -f "icons.json" ] || [ $(find svg -name "*.svg" 2>/dev/null | wc -l) -eq 0 ]; then
            echo "Icons not found, generating..."
            npm run transform-svg || echo "Transform failed"
            npm run generate-components || echo "Generate components failed"
            npm run generate-outputs || echo "Generate outputs failed"
          else
            echo "Icons already exist"
          fi

      - name: Copy icons to docs before build
        run: |
          echo "Copying icons.json..."
          cp icons.json docs/ || echo "icons.json not found"
          echo "Copying SVG files..."
          mkdir -p docs/svg
          cp -r svg/*.svg docs/svg/ 2>/dev/null || echo "No SVG files to copy"
          echo "Files in docs/:"
          ls -la docs/ | grep -E "(icons.json|svg)"
          echo "Files in docs/svg/:"
          ls -la docs/svg/ 2>/dev/null || echo "No svg directory"

      - name: Build preview site
        run: npm run build:docs

      - name: Verify dist directory
        run: |
          echo "Checking docs/dist/..."
          ls -la docs/dist/
          echo "Checking for icons.json..."
          ls -la docs/dist/icons.json || echo "icons.json missing in dist"
          echo "Checking for svg directory..."
          ls -la docs/dist/svg/ || echo "svg directory missing in dist"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs/dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
